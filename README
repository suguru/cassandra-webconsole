Cassandra-WebConsole is web based console for cassandra server cluster.

Project description
-------------------


Requirements
-------------------
  * Cassandra >= trunk (does not work with 0.6)
  * Java >= 1.6
  * Tomcat >= 6.0
    maven2 (compile)

Getting started
-------------------
put the WAR file to your web application directory.
Configuration menu will be shown at first access.
